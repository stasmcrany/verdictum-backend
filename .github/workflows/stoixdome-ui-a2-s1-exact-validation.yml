name: STOIXDome runner probe

on:
  push:
    branches:
      - validation/stoixdome-ui-a2-s1-20260919

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - run: echo STOIXDOME_RUNNER_PROBE_PASS
